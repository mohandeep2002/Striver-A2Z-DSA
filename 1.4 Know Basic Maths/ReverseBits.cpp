#include <bits/stdc++.h>
using namespace std;

long reverseBits(long n)
{
}

int main()
{
    int T;
    cin >> T;
    long n;
    while (T--)
    {
        cin >> n;
        cout << reverseBits(n);
    }
    return 0;
}